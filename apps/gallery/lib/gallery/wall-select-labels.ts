/** aria-label for the overflow "More" menu on a wall/manage selection. */
export function describeMoreSelectionActionsAriaLabel(): string {
  return "More selection actions"
}

/** aria-label for the bulk-tag input on the wall select bar. */
export function describeTagToApplyAriaLabel(): string {
  return "Tag to apply to selection"
}

/** Placeholder for the bulk-tag input. */
export function describeTagDraftPlaceholder(): string {
  return "Tag…"
}

/** aria-label for the new-album title input on the wall select bar. */
export function describeNewAlbumTitleAriaLabel(): string {
  return "New album title"
}

/** Placeholder for the new-album title input. */
export function describeAlbumTitlePlaceholder(): string {
  return "Album title…"
}

/** Placeholder for bulk-tag example chips on manage. */
export function describeBulkTagExamplesPlaceholder(): string {
  return "retreat, axolotl…"
}

/** Placeholder for a tag slug input (untag / filter). */
export function describeTagSlugPlaceholder(): string {
  return "slug…"
}

/** Disabled wall-select title / label when the viewer is signed out. */
export function describeSignInToCurateLabel(): string {
  return "Sign in to curate"
}
