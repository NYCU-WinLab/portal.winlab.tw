/** aria-label for the keyboard shortcuts cheatsheet trigger / dialog. */
export function describeKeyboardShortcutsAriaLabel(): string {
  return "Keyboard shortcuts"
}

/** aria-label for closing the keyboard shortcuts cheatsheet. */
export function describeCloseShortcutsAriaLabel(): string {
  return "Close shortcuts"
}

/** aria-label for the memories day navigator. */
export function describeChooseCalendarDayAriaLabel(): string {
  return "Choose a calendar day"
}

/** aria-label for jumping to the previous memories day. */
export function describePreviousDayAriaLabel(): string {
  return "Previous day"
}

/** aria-label for jumping to the next memories day. */
export function describeNextDayAriaLabel(): string {
  return "Next day"
}
