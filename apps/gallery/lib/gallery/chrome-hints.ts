/** Hint under the default paper-wall seasonal theme option. */
export function describePaperWallThemeHint(): string {
  return "Default darkroom renewal"
}

/** Hint under limited-time seasonal theme options. */
export function describeLimitedSeasonalThemeHint(): string {
  return "Limited-time overlay"
}

/** aria-label while clearing broken media-health selections. */
export function describeClearBrokenShotsAriaLabel(): string {
  return "Clear all selected broken shots"
}

/** Compact Clear all label on media health. */
export function describeMediaHealthClearAllLabel(): string {
  return "Clear all"
}

/** Wall select-mode status hint under the infinite wall. */
export function describeWallSelectModeHint(): string {
  return "Select mode · Shift+click for ranges · bulk tools below"
}
