/** Visible label while a Retry control is loading. */
export function describeRetryingLabel(): string {
  return "Retrying…"
}

/** Visible label while sign-out is in flight. */
export function describeSigningOutLabel(): string {
  return "Signing out…"
}

/** Visible label while a Save control is in flight. */
export function describeSavingLabel(): string {
  return "Saving…"
}

/** Visible label while a Create control is in flight. */
export function describeCreatingLabel(): string {
  return "Creating…"
}

/** Visible label while OAuth redirect is starting. */
export function describeRedirectingLabel(): string {
  return "Redirecting…"
}

/** Visible label while album share is in flight. */
export function describeSharingLabel(): string {
  return "Sharing…"
}
