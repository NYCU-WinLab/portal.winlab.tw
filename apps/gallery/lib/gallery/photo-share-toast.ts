/** Toast after copying a photo deep link to the clipboard. */
export function describePhotoLinkCopied(): string {
  return "Link copied."
}

/** Toast after a successful original-file download. */
export function describeSavedOriginal(): string {
  return "Saved original"
}
