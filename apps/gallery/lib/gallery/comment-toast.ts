/** Toast after posting a gallery comment. */
export function describeCommentPosted(): string {
  return "Comment posted."
}

/** Toast after deleting a gallery comment. */
export function describeCommentDeleted(): string {
  return "Comment deleted."
}

/** Toast after editing a gallery comment. */
export function describeCommentUpdated(): string {
  return "Comment updated."
}
