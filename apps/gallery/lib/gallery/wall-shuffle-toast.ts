/** Toast after shuffling the wall order client-side. */
export function describeWallReshuffled(): string {
  return "Wall reshuffled."
}

/** Toast after restoring load order after a shuffle. */
export function describeWallOrderRestored(): string {
  return "Wall order restored."
}
