/** aria-label for dismissing the PWA install prompt. */
export function describeDismissInstallPromptAriaLabel(): string {
  return "Dismiss install prompt"
}

/** aria-label for the seasonal site theme radio group. */
export function describeSeasonalSiteThemeAriaLabel(): string {
  return "Seasonal site theme"
}

/** Idle label for the PWA install action. */
export function describeInstallLabel(): string {
  return "Install"
}
