/** aria-label for the albums list search form. */
export function describeSearchAlbumsAriaLabel(): string {
  return "Search albums"
}

/** aria-label for the albums search input. */
export function describeSearchAlbumsInputAriaLabel(): string {
  return "Search albums by title, slug, or owner"
}

/** aria-label for clearing the albums search draft. */
export function describeClearAlbumSearchAriaLabel(): string {
  return "Clear album search"
}

/** Placeholder for the albums search input. */
export function describeAlbumsSearchPlaceholder(): string {
  return "Search title, slug, owner…"
}
