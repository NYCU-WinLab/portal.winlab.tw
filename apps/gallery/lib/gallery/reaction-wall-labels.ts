/** aria-label for opening who reacted to a photo. */
export function describeShowWhoReactedAriaLabel(): string {
  return "Show who reacted"
}

/** aria-label for the reaction picker control. */
export function describeChooseReactionAriaLabel(): string {
  return "Choose a reaction"
}

/** aria-label for the main gallery wall region. */
export function describeGalleryWallAriaLabel(): string {
  return "Gallery wall"
}
